# 커피숍
menu = {"커피", "우유", "쥬스"}
print(menu, type(menu))

menu = list(menu) # list 로 변환
print(menu, type(menu))

menu = tuple(menu) # tuple 로 변환
print(menu, type(menu))

menu = set(menu) # set 로 변환
print(menu, type(menu))